// import { format, compareAsc } from 'date-fns'

// document.getElementById('task-form').addEventListener('submit', function(e) {
//     e.preventDefault();  // Prevent the default form submission
    
//     const dueDateInput = document.getElementById('due-date');
//     const dueDate = new Date(dueDateInput.value);  // Get the date from the input

//     // Example: Format the date in a readable format
//     console.log("Formatted Date:", format(dueDate, 'MMMM do, yyyy'));

//     // You can perform other date operations as needed here
// });
